# Quadratic-Placement-in-Python
This Python tool implements a Recursive Quadratic Placement (QP) engine for VLSI physical design. It solves the wirelength minimization problem by formulating it as a system of linear equations ($Ax = b$) using a clique model. The engine features recursive subregion partitioning (slicing) and pseudo-pad propagation to handle boundary constraints.
